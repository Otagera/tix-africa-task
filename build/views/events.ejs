<%- include("./partials/header.ejs")%>
<section id='events' class="layout-wrapper d-lg-flex flex-column mx-auto my-5 py-5 w-75 border rounded bg-light">
    <h1 class="text-center col-sm mt-3">Events</h1>
    <div>
        <ul>
            <%for(let i = 0; i < events.length; i++){ %>
                <li class="d-flex flex-row justify-content-between">
                    <div>
                        <p>Name: <%=events[i].name%></p>
                        <p>Date: <%=events[i].timeOfEvent.toGMTString().substr(0, 16)%></p>
                        <p>Time: <%=events[i].timeOfEvent.toTimeString().substr(0, 5)%></p>
                        <p>Event Type: <%=events[i].eventType%></p>
                        <p>Creator: <%=events[i].creator%></p>
                    </div>
                    <div class="p-5">
                        <a class="p-5" href="/event/edit/<%=events[i]._id%>">Edit</a>
                        <p class="p-5">
                            <% if(Date.now() < new Date(events[i].timeOfEvent).getTime()){ %>
                                Upcoming
                            <% }else{ %>
                                Held
                            <% } %>
                        </p>
                    </div>
                </li>
                <hr class="mt-2 mb-3 mx-5" />
            <% } %>
        </ul>
    </div>

    <!-- Start Login Error Modal -->
    <div class="modal fade" id="login-error" tabindex="-1" role="dialog" aria-labelledby="login-error" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title font-size-16" id="addContact-exampleModalLabel">Login Not Successful</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <p>Username/Password is incorrect, please check and retry again.</p>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
    <!-- End Login Error Modal -->
</section>
<%- include("./partials/footer.ejs", { jsFile: 'events' })%>
